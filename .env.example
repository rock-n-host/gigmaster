# Gigmaster environment example

# Application
NODE_ENV=development
PORT=3000

# Backend: where lyrics are stored (mounted volume / local folder)
# Default is ./lyrics relative to repo root
LYRICS_DIR=./lyrics

# Frontend
# Vite uses VITE_BACKEND_URL to call the API when running in dev
VITE_BACKEND_URL=http://localhost:3000
